import { FilterByTagPipe } from './filter-by-tag.pipe';

describe('FilterByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByTagPipe();
    expect(pipe).toBeTruthy();
  });
});
